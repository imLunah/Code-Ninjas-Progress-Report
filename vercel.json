{
  "version": 2,
  "buildCommand": "npm install --prefix server && npm install --prefix client && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
