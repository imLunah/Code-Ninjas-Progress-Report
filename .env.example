SESSION_SECRET=change-me-to-something-secret
PORT=3001
DB_PATH=./server/data/codeninjas.db

# MyStudio integration (experimental). Encrypts the stored MyStudio session
# cookie at rest. 32 bytes, hex:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MYSTUDIO_ENC_KEY=
