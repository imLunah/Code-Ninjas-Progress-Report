// Feature flags. Flip to true to expose a feature to all staff.

// Getting Started / new-staff onboarding walkthrough. Still in progress — keep
// false to hide every entry point (auto-redirect + Account card). The
// /getting-started route stays reachable by direct URL for testing.
export const ONBOARDING_ENABLED = false;
